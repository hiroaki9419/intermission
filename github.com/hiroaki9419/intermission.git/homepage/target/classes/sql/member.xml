<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="member">
	
	<!-- 아이디 중복 검사 -->
	<select id="check_id" parameterType="String" resultType="int">
		select count(*) from member where user_id = #{id}
	</select>
	
	<!-- 이메일 중복 검사 -->
	<select id="check_email" parameterType="String" resultType="int">
		select count(*) from member where user_email = #{email}
	</select>
	
	<!-- 회원 가입 -->
	<insert id="join_member" parameterType="member">
		insert into member values(#{id}, #{name}, #{password}, #{email}, now(), #{picture}, 'false', #{approvalKey})
	</insert>
	
</mapper>
